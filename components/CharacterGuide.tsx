"use client";

type CharacterGuideProps = {
  mood?: "curious" | "excited" | "celebrating";
  message: string;
  label?: string;
  className?: string;
};

export default function CharacterGuide(_props: CharacterGuideProps) {
  return null;
}
